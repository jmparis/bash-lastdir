# BEGIN lastdir

# retrieve current fodler's path
lastdir=`pwd`

# substract the HOME directory path from current path
# and save the final relative path
echo ${lastdir#${HOME}} > ~/.lastdir

# END lastdir